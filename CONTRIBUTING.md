# Contributing

Run tests with `pip install -e ".[dev]" && pytest`.

Provenance tests fail a PR that overwrites source bytes or drops occurrence rows.

After the first external PR, enable required owner review on `main`. Until then, required CI is the review gate.
